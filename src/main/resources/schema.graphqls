type Query {
    user(uid: ID): User
}

type User {
    uid: ID,
    username: String,
    courses: [Course]
}

type Course {
    name: String,
    passingGrade: Int,
    marks: [Mark],
    grade: Int
}

type Mark {
    weight: Int,
    mark: Int
}