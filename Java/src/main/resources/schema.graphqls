type Query {
    user(uid: ID): User
}

type User {
    uid: ID,
    courses: [Course]
}

type Course {
    name: String,
    passingGrade: Int,
    marks: [Mark],
    grade: Int
}

type Mark {
    name: String,
    weight: Int,
    mark: Int
}